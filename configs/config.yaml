experiments:
  - name: baseline
    batch_size: 16
    epochs: 3
    learning_rate: 0.001

  - name: low_lr
    batch_size: 16
    epochs: 3
    learning_rate: 0.0001

  - name: large_batch
    batch_size: 32
    epochs: 3
    learning_rate: 0.001

data:
  path: "data/htr_input_sample.json"

model:
  name: retrieval
  
  
